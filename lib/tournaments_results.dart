import 'package:flutter/material.dart';

class TournamentsResultsPage extends StatelessWidget {
  const TournamentsResultsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Turnuvalar ve Sonuçlar'),
      ),
      body: const Center(
        child: Text('Turnuvalar ve Sonuçlar içeriği buraya gelecek'),
      ),
    );
  }
}
