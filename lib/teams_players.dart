import 'package:flutter/material.dart';

class TeamsPlayersPage extends StatelessWidget {
  const TeamsPlayersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Takım ve Oyuncular'),
      ),
      body: const Center(
        child: Text('Takım ve Oyuncular içeriği buraya gelecek'),
      ),
    );
  }
}
